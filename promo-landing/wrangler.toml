# Deploy: npm run promo:deploy (from repo root)
# First time: npx wrangler login
name = "keylounge-promo"
compatibility_date = "2024-11-01"

[assets]
directory = "."
not_found_handling = "404-page"
html_handling = "auto-trailing-slash"
